
apply plugin: "info.solidsoft.pitest"


pitest {
    targetClasses = ['pl.java.scalatech.pi.*']  //by default "${project.group}.*"
    pitestVersion = "1.1.0" //not needed when a default PIT version should be used
    threads = 4
    outputFormats = ['XML', 'HTML']
}